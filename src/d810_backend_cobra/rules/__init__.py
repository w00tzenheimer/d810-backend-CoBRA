"""Optimizer rules contributed to d810 by this backend.

Registered via the ``rules`` field of the package MANIFEST: d810 imports these
after the backend probes usable, because its own rule scan is path-scoped to
``d810.optimizers`` and cannot see them.
"""
